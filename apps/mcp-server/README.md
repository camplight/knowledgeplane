# knowledgeplane-server

Fastify-based MCP memory server. See root README for quickstart.

- ENV: `DATABASE_URL`, `PORT`, `API_KEYS`, `EMBEDDINGS_PROVIDER`, `OPENAI_API_KEY`
- Run: `npm run dev`
- Docs: `http://localhost:8080/docs`

